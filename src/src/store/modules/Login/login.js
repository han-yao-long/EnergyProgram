import axios from 'axios';
const state = {
    username: localStorage.getItem('ms_username')
};
const mutations = {


};

const actions = {
   

    };
    
export default {
    state,
    actions,
    mutations
};

